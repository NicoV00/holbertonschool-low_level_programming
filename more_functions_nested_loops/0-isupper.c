#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase character.
 * @a: int
 * Return: 1: c is uppercase. 0: is lowercase.
 */
int _isupper(int a)
{
	if (isupper((a)
		return (1);
    return (0);
}
