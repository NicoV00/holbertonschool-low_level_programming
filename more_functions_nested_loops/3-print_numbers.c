#include "main.h"

/**
 * print_numbers - Prints numbers from 1 to 10
 *
 * Description: size
 *
 * Return: always 0
 */
void print_numbers(void)
{
int i;

	for (i = 0; i < 10; i++)
		_putchar(i);

	_putchar(10);
}
